class_name Footman
extends BaseUnit

func _ready() -> void:
	super._ready()

func _process(delta: float) -> void:
	super._process(delta)

# --- GESTIONE ANIMAZIONI ---
	#Non avendo particolari animazioni, utilizzo quelle codificate 
	#nella classe generica
	
